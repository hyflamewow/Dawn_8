import { WeatherForecastElf } from './weather-forecast-elf';

describe('WeatherForecastElf', () => {
  it('should create an instance', () => {
    expect(new WeatherForecastElf()).toBeTruthy();
  });
});
